import { ChainId as ChainId_ } from '@uniswap/sdk'

export enum ChainId {
  GÖRLI = ChainId_.GÖRLI,
  MAINNET = ChainId_.MAINNET,
  ROPSTEN = ChainId_.ROPSTEN,
  RINKEBY = ChainId_.RINKEBY,
  KOVAN = ChainId_.KOVAN,
  POLYGON = 137
}
